{
  "name": "to-do-list",
  "version": "1.0.0",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm i )|| (cd client && npm i)",
    "start": " if-env TYPE=server && (node ./server/server.js) || (cd client && npm start)",
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
